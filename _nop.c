#include "monty.h"

/**
 * nop - nothing
 * @stack: top of the stack.
 * @line_number: line_number.
 * Return: NOTHING
*/

void _nop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_cnt;
}
